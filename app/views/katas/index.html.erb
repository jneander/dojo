<h3>Available Katas</h3>

<table class='table table-striped clickable_rows'>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Last Updated</th>
    </tr>
  </thead>

  <tbody>

    <% @katas.each do |kata| %>
      <tr data-href='<%= kata_path(kata.id) %>'>
        <td><%= kata.title %></td>
        <td><%= kata.description %></td>
        <td><%= KatasHelper::Formatter.timestamp(kata.last_updated) %></td>
      </tr>
    <% end %>

  </tbody>
</table>
